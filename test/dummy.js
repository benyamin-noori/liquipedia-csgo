/* global describe it */
const CounterStrike = require('../src');
// eslint-disable-next-line no-unused-vars
const assert = require('assert');

describe('Dummy test suite', () => {
	// eslint-disable-next-line no-unused-vars
	const cs = new CounterStrike();
	it('should run the dummy test', () => {
	});
});
